const fp = require("fastify-plugin");
const fastify = require('fastify')()

module.exports = fp(async function (fastify, opts) {
    // fastify.register(require('@fastify/mysql'), {
    //     connectionString: fastify.config.MYSQL_DB_URL
    // })
})
